<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<data>
		<record id="wizard_message_stock_rules_form" model="ir.ui.view">
            <field name="name">message.stock.rules</field>
			<field name="model">message.stock.rules</field>
			<field name="type">form</field>
            <field name="arch" type="xml">
                <form>
                    <separator/>
                    <h3>¿Do you want to consolidate replenishment rules?</h3>
                    <separator/>
                    <footer>
                        <button name="confirm" string="confirm" type="object" class="btn-secondary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>

		<record id="action_message_stock_rules" model="ir.actions.act_window">
            <field name="name">Replenishment rules</field>
            <field name="res_model">message.stock.rules</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
	</data>
</odoo>
